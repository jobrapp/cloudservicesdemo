package com.jobrapp.cloudservicesdemo.data

/**
 * Store the different types of sources we support
 */
enum class Sources {
    GOOGLE_DRIVE,
    GMAIL,
    DROPBOX,
    BOX,
    ONEDRIVE,
    DEVICE
}